import HeroSection from '@/components/landing/hero'; // Assuming '@' is configured for src path

export default function LandingPage() {
  return (
    <main>
      <HeroSection />
      {/* Other sections of the landing page will go here */}
    </main>
  );
}
